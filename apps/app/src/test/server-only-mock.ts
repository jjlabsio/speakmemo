// Vitest stub for the `server-only` package.
// The real `server-only` package throws at runtime when imported outside a
// Next.js server environment. This empty stub lets Vitest load server modules
// without triggering that guard.
export {};
